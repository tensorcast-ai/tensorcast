[pytest]
norecursedirs =
    bazel-*
    external
    .venv
    build
    dist
    node_modules
testpaths =
    tests
addopts = -q
markers =
    integration: marks integration-style tests (may require external deps)
asyncio_default_fixture_loop_scope = function
